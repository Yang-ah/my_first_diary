import { LineHeader } from "../../components";
import styles from "./scheduler.module.scss";

const Scheduler = () => {
  return (
    <main>
      <LineHeader />
    </main>
  );
};

export default Scheduler;
